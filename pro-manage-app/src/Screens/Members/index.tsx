import React from 'react';
import { ScrollView, Text, View } from "react-native";
import styles from "./styled.tsx";

export default ({ navigation, route }: any) => {
  
  return (
    <View style={styles.Container}>
      <Text > Detalhe membros </Text>
    </View>
  );
};
import React from 'react';
import { ScrollView, Text, View } from "react-native";
import styles from "./styled.tsx";

export default () => {
  
  return (
    <View style={styles.Container}>
      <Text > Inf Partner </Text>
    </View>
  );
};
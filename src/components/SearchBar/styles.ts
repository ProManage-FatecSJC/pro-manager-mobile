import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'center',

      padding: 8,
      marginTop: 16,
      marginBottom: 32,

      borderBottomWidth: 2,
      borderBottomColor: '#00688C',
    },
    input: {
      flex: 1,
      fontSize: 18,
    },
  });